import Navbar from "./navbar"

const Header = () => {
    return (
        <header className="right-0 top-0 left-0 z-50 bg-transparent p-3 sm:p-4 lg:p-5 inline-flex fixed w-full sm:w-auto">
            <Navbar />
        </header>
    )
}

export default Header;